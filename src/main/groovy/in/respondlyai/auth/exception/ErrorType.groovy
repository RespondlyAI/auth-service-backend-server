package in.respondlyai.auth.exception

enum ErrorType {

    VALIDATION_ERROR,
    AUTH_ERROR,
    FORBIDDEN,
    NOT_FOUND,
    CONFLICT,
    DATABASE_ERROR,
    BAD_REQUEST,
    INTERNAL_SERVER_ERROR,
    SERVICE_UNAVAILABLE

}
